Drop hardware photos here (e.g. `bosch_plr40c.jpg`, `jrt_b605b.jpg`).

In `report.tex`, replace the matching `\photoplaceholder{...}{...}` call with:

```latex
\includegraphics[width=0.6\linewidth]{images/<filename>}
```
