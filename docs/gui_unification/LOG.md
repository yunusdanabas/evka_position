# EVKA GUI Unification Log

Most recent first. Single-agent implementation unless this file states otherwise.

| Date | Agent | Entry |
|---|---|---|
| 2026-07-16 | Cursor Agent | **Removed `tools/evka_gui_v2`**: deleted the deprecated compatibility shim package; docs updated (`README`, `HANDOFF`, `CONTRIBUTING`, `tools/README`, `CMD_SOFTWARE_INTEGRATION`) to drop v2 entry-point references. Canonical GUI remains `python -m tools.evka_gui`. |
| 2026-07-14 | Claude Code (Opus 4.8) | **Web 50/50 layout + two bug fixes; flashed to hardware**. Landscape dashboard now mirrors the desktop split: 3D canvas over a horizontal XY/XZ/YZ strip on the left half, panel on the right half in two sub-columns (`.panel-col`) so nothing scrolls at 1080p; portrait canvas 55vh→45vh (sub-columns stack). **Bug: IPT panel never appeared in a real browser** — `toggleIpt()` cleared the inline style, falling back to the stylesheet's `display:none`; caught by hardening the jsdom harness to check *computed* style (the old check compared the inline string and passed). **Bug: Disconnect didn't stick** — both reader threads emitted an unconditional disconnect event from `finally`, even on a requested stop; with auto-reconnect this re-connected the GUI ~1 s after an explicit Disconnect. Fixed in `transport.py` + `tcp_client.py` (suppress on requested stop) and guarded in `_drain` (ignore disconnect events when `_transport is None`); `tests/test_disconnect.py` pins both against a real localhost socket, mutation-checked. Verify: 74 pytest passed; harness 29/29; `pio run -e esp32s3_v4` SUCCESS; **flashed to /dev/ttyACM0** (hash verified), 20 Hz DATA + `ACK:PONG` confirmed post-flash. |
| 2026-07-14 | Claude Code (Opus 4.8) | **Desktop↔web unification, Phase 3 — one product**: design system published to Claude Design (project *EVKA Operator UI*: colour roles + action vocabulary). `docs/gui_unification/DESIGN_TOKENS.md` is the spec; `tools/evka_gui/tokens.py` and a CSS `:root` block carry the values. Desktop's Flat-UI palette (`#27ae60`/`#c0392b`/`#f39c12`/`#2980b9`) remapped onto the canonical HUD roles (76 hexes). Action vocabulary applied: desktop stopped using raw protocol tokens as labels (`SAVE_POINT` → **SAVE POINT**, `DEL_POINT` → **DEL LAST POINT**), `Hardware ZERO` → **ZERO (HW)**, etc.; web's own collisions fixed (`SAVE ORIGIN`/`SET ORIGIN` for one concept → **SET ORIGIN**; a bare ambiguous `CLEAR` → **CLEAR TRAIL**). PLAN.md drift corrected (it still claimed "without tab switching" and a "protocol log dock"; neither has been true since the 2026-07-14 layout pass). |
| 2026-07-14 | Claude Code (Opus 4.8) | **Desktop↔web unification, Phase 2 — parity**: **Quick IPT on the web** — `tools/ipt/solver.py` ported to JS inside the blob (Coope seed, fixed-radius trilateration, Gauss-Newton, geometry-cond gate) with a ~52-line linear-algebra kernel (equilibrated normal equations + Cholesky w/ ridge fallback; cyclic Jacobi for the cond). IPT is a top-level **mode**, not a view — CALIBRATE hides the canvases, and watching the cloud grow while sweeping is the point. Capture taps raw `x,y,z` in the `DATA,` branch (sensor-frame, PLAN.md:51), `iptArm()` force-unfreezes (capture sits behind `if(frozen)return`). **Recording on both UIs** — needed no new format: `load_replay_frames` already reads raw `DATA,` dumps, so the phone records and the desktop replays it unconverted; TCP (which sends `X,Y,Z`+`SENSOR`) is re-serialised. Recording taps the wire, FREEZE only pauses the view. Web also gained the **protocol log** (native `<details>`, ring buffer, DATA/ACK/ERR filters) and **RAW_COUNTS**. Snapshot CSV gained a `Frame` column so IPT (sensor) and manual (display/soft-zeroed) rows can never silently mix. Verify: `pio run -e esp32s3_v4` SUCCESS (flash 26.7%); 70 pytest passed. |
| 2026-07-14 | Claude Code (Opus 4.8) | **Desktop↔web unification, Phase 1 — correctness**: **fixed a data-loss bug** — the web's Theta/Phi calibration tabs sent `SET_PPR_ROTARY` but never `SAVE_PPR`, so a rotary calibration done from a phone was silently lost on the next reboot (Wire had APPLY+SAVE; the rotary tabs did not). Desktop gained **auto-reconnect** (exponential backoff 1→30 s, Disconnect doubles as Cancel — there was previously *no* reconnect on any transport), **FREEZE** (gates the position stream only; command replies still land, matching the web), and **3D wheel zoom** (`_View3D._cam_dist`, plus a behind-camera divide guard). WS command queue 4→8 (it drains one command per `loop()`, and the 5 s poll alone fires three). |
| 2026-07-14 | Claude Code (Opus 4.8) | **Verification infrastructure**: `tools/ipt/tests/test_web_solver_parity.py` — slices the marker-delimited, DOM-free JS solver out of `WebDashboard.cpp`, runs it under node, and asserts it matches `tools/ipt/solver.py` on the same clouds (P within 1e-6 mm, warning strings exact) plus a no-node layer that compares thresholds. Mutation-checked: deleting the Coope mean add-back fails 5 cases. `tools/webdash_harness/` — runs the *real* dashboard page under jsdom with a mocked WebSocket and a synthetic spiral sweep, because `pio run` proves the blob compiles and nothing about whether the JS runs. 29 checks; IPT recovers a known hidden target to **0.014 mm** in-page. |
| 2026-07-14 | OpenCode (GPT-5.5) | **evka_gui no-dock/no-scroll layout pass**: moved protocol log from bottom dock into a compact left-column panel, removed the left `QScrollArea`, moved IPT / Points / Diagnostics / Commands into tabs, moved Battery and ESP-NOW remote into Diagnostics, and moved right-column controls above the plot grid so the bottom/right area remains plots. Compact connection panel now permits 50/50 split at 1366×768 and 1920×1080. Verify: `pytest tools/evka_gui/tests -q` 34 passed; `python -m compileall tools/evka_gui -q`; offscreen 1920×1080 split `[957, 957]`, 1366×768 split `[680, 680]`; no `QDockWidget`, left panel is `QWidget`. |
| 2026-07-14 | OpenCode (GPT-5.5) | **evka_gui full-page polish + bug pass**: restored true 50/50 maximized splitter behavior with horizontally expanding control panel, enlarged/stretch-based saved-points list, compact 160px protocol-log dock, and 2D origin/saved-point layer markers. Fixed protocol log UI-thread updates, TCP XYZ log classification, WebSocket connect timeout/state cleanup, malformed `POINT` parsing, rejected IPT solution action gating, and Remote Tester TCP thread cleanup. Added focused tests for protocol classification, WebSocket URL parsing, control-line ingestion, saved-points CSV, and profiles. Docs updated for inline Quick IPT + WebSocket examples. Verify: `pytest tools/evka_gui/tests -q` 34 passed; `python -m compileall tools/evka_gui -q`; offscreen `EvkaWindow`/`RemoteTesterWindow` smoke OK; offscreen 1920×1080 split `[957, 957]`, 1366×768 split `[680, 680]`. |
| 2026-07-14 | Cursor Agent | **Main panel layout de-clutter**: left column max 540px; splitter favors views (480/1120, stretch 0:1); Commands / Points / Diagnostics tabs replace always-visible SYSINFO strip + horizontal command row + tall saved-points block; IPT panel spacing tightened; 3D/2D grid row/column stretch. Protocol log dock + replay bar (replay-only) unchanged. Tests: 27 passed; offscreen `EvkaWindow`. |
| 2026-07-14 | Cursor Agent | **Remote tester layout fix**: `remote_test_gui.py` — `_header_widget()` hook + log `stretch=1`; `remote_window.py` dev note via hook (not `insertWidget(0)`); min 480×560 / default 520×680 for banner headroom. |
| 2026-07-14 | Cursor Agent | **Phase 6 — Remote tester window**: `tools/evka_gui/remote_window.py` wraps `tools/remote_tester/remote_test_gui.py` as toolbar **Remote Tester…** (separate TCP to ButtonRemoteTest AP `192.168.4.1:8080`; not production EVKA session). Standalone `python tools/remote_tester/remote_test_gui.py` unchanged; README notes evka_gui toolbar entry. |
| 2026-07-14 | Cursor Agent | **Phase 5 — Calibration JSON workflow**: `calibration.py` endpoint tab — **Import CSV** (world/sensor pairs), **Write calibration.json** via `kabsch()` from `tools/calibration/calibrate.py` (schema `R`, `t`, `rmse_mm`, `n_points`). Live GUI transform from JSON deferred (legacy `position_checker/main.py` only). |
| 2026-07-14 | Cursor Agent | **Phase 4 — Replay controls**: replay bar in `gui.py` — play/pause, speed 0.25×–4× (`replay_utils.interval_for_speed`), step ±1, slider seek with `_replay_seek()` replay-from-zero (rebuilds trail/minmax). Visible when Replay CSV connected. |
| 2026-07-14 | Cursor Agent | **Phase 3 — IPT depth**: `ipt_panel.py` — Copy P, Export Capture CSV, Add P to Snapshots; sweep hints during ARM/SOLVE using `MIN_POINTS`, `COND_WARN`, `COND_BLOCK` from `tools/ipt/solver.py`. `_View3D` in `position_checker/gui.py` — `set_ipt_overlay()` for cloud/marker/sphere wireframes on software 3D view; gated by IPT layer toggle. |
| 2026-07-14 | Cursor Agent | **Phase 2 — Transport & diagnostics**: `ws_client.py` (QWebSocket, port 80 `/ws`); 4th connection radio + `--ws HOST` in `__main__.py`. `protocol_log.py` bottom dock (DATA/ACK/ERR/Other filters, pause, clear). Toolbar Open Dashboard (`webbrowser`). CONSTANTS/PPR strip + raw counts strip on main panel; `RAW_COUNTS` → `RAW,<θ>,<φ>,<wire>` in `firmware/src/EvkaPosition.cpp`; `model.py` parses `RAW,`. `RAW_COUNTS` in quick commands. Docs: `README.md`, `CMD_SOFTWARE_INTEGRATION.md`. |
| 2026-07-14 | Cursor Agent | **Phase 1 — Session & polish**: snapshots (capture/clear/export CSV, display-frame coords); saved-points CSV export; saved-point labels refresh on SW zero (`QListWidgetItem.UserRole`). Plot layer toggles, trail max spinbox, shortcuts `Z`/`S`/`A`/`Esc`, copy coords, threshold alerts. Connection profiles (save/load/delete). Helpers: `session_utils.py`. Tests: `test_session_utils.py`, `test_replay_utils.py`. |
| 2026-07-14 | Cursor Agent | **Inline IPT + WiFi window**: `ipt_panel.py` inline on main page; IPT overlays on XY/XZ/YZ; `wifi_window.py` for credentials off main panel; compact SYSINFO strip; `IptPlotsWindow` optional pop-out (`ipt_window.py`). Tests: `test_ipt_panel.py`. |
| 2026-07-14 | Cursor Agent | **Quick IPT in evka_gui** (initial): toolbar secondary window fed from `_drain`; reuses `tools/ipt` capture/solver; `test_ipt_window.py`. Doc sweep across README, HANDOFF, CONTRIBUTING, PLAN, CMD integration, v4 FIRMWARE, ipt README. |
| 2026-07-14 | Cursor Agent | **Verification (enhancement batch)**: `pytest tools/evka_gui/tests -q` → 27 passed; `pio run -e esp32s3_v4` OK; offscreen `EvkaWindow` instantiate OK. New modules: `session_utils.py`, `protocol_log.py`, `ws_client.py`, `replay_utils.py`, `remote_window.py`, `ipt_panel.py`, `wifi_window.py`, `ipt_window.py`. |
| 2026-07-09 | OpenCode | **Final polish**: fixed dead conditional in `calibration.py:241` (removed redundant ternary — firmware uses single `SET_PPR_ROTARY` for both axes); fixed JS indentation in `WebDashboard.cpp` `onWsMessage` handler (SYSINFO/BATT/POINT blocks). All checks pass: `pytest -q` (62 passed), `compileall`, `pio run` (esp32s3_v4, wemos_d1_r32, button_remote). |
| 2026-07-09 | OpenCode | Phase 1 complete. Canonical GUI is `tools/evka_gui` with `python -m tools.evka_gui` and `evka-gui`; `tools/evka_gui_v2` now contains deprecation shims. Added unified GUI operator parity, calibration capture helpers, TCP/WebSocket `BATT` parity, WebDashboard battery polling/display, Windows CMD `BATT` parsing, and docs updates. Verification passed: `pytest tools/evka_gui/tests -q`, `python -m compileall tools/evka_gui tools/evka_gui_v2 tools/position_checker`, offscreen Qt instantiate, `pytest -q` (62 passed), `pio run -e esp32s3_v4`, and `dotnet build CMDScanner.csproj` (success with existing nullability warnings). |
| 2026-07-09 | OpenCode | Phase 0 audit completed. Approved decisions captured: canonical package `tools/evka_gui`, command `python -m tools.evka_gui`, single-agent implementation, and TCP/WebSocket `BATT` parity. Implementation started. |
